# URL base de la API del backend (sin trailing slash)
VITE_API_URL=http://127.0.0.1:8000/api/v1

# ID del hotel al que pertenece esta instancia del frontend
# Cada hotel tiene su propio despliegue con su hotel_id configurado aquí
VITE_HOTEL_ID=1
